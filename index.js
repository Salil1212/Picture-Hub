// Create a function that will return all possible permutations and it take input as a string
// string="hello"
// using python

// func(str):
// n=str.length
// li=[str[i] for in range(str.size())]
// for permute in permutation(li,n):
// str=permute.join()

// str=input("Enter the string")
// func(str)
